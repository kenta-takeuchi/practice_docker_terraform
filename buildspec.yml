version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  build:
    commands:
      - build_docker_image.sh
  post_build:
    commands:
      - push_to_ecr.sh
artifacts:
  files: imagedefinitions.json